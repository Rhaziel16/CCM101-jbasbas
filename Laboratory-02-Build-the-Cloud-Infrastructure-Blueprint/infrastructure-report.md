# Infrastructure Report

## Operating System

The operating system used by the server is **PRETTY_NAME="Ubuntu 24.04.4 LTS"**.

I checked this using the `cat /etc/os-release` command.

## Kernel Version

The kernel version is **6.8.0-136-generic**.

I checked this using the `uname -r` command.

## CPU Model

The CPU model is **[PUT YOUR CPU MODEL HERE]**.

I used the `lscpu` command to check the processor information.

## Number of CPU Cores

The server has **1 CPU cores**.

I used the `nproc` command to check the number of available CPU cores.

## Total RAM

The server has **[PUT YOUR RAM HERE]** of total memory.

I checked this using the `free -h` command.

## Disk Capacity

The available disk information is **[PUT YOUR DISK INFORMATION HERE]**.

I used the `df -h` command to check the disk capacity and usage.

## Mounted File Systems

The mounted file systems shown by the server include:

- [PUT RESULT HERE]
- [PUT RESULT HERE]
- [PUT RESULT HERE]

I used the `df -h` command to view the mounted file systems.

## Hostname

The hostname of the server is **ubuntu**.

I checked this using the `hostname` command.

## IP Address

The IP address of the server is **172.30.1.2 172.17.0.1 **.

I checked this using the `hostname -I` command.

## Summary

The KillerCoda server provides the basic resources needed to run a Linux environment. By checking the CPU, memory, storage, network, and operating system, I was able to get a better idea of what resources are available in a cloud server.
